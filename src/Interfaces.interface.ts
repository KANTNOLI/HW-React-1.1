export interface Cart {
  imageUrl: string;
  title: string;
  description: string;
}

export interface User {
  name: string;
  email: string;
  avatarUrl: string;
}

export interface CartOzonIfc {
  img: string;
  title: string;
  price: number;
  disc: number;
  count: number;
}
